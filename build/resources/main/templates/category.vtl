<h1>$category.getName()</h1>

<ul>
    #foreach($task in $category.getTasks())
    <li>$task.getTitle()</li>
    #end
</ul>

<a href="/categories/$category.getID()/tasks/new"><button class="btn btn-danger">Add a new task</button></a>
<a href="/categories"><button class="btn btn-info">All categories</button></a>